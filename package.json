{
  "name": "capacitor-live-reload-bug",
  "version": "1.0.0",
  "description": "A simple reproduction for the Capacitor 5.7.2 Android live reload bug",
  "scripts": {
    "start:server": "browser-sync start --server src --files src --port 3000 --no-notify --no-open",
    "start:android": "npx cap run android -l external"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.2",
    "@capacitor/core": "^5.7.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.2",
    "browser-sync": "^3.0.2",
    "typescript": "^5.4.2"
  },
  "author": "",
  "license": "ISC"
}
